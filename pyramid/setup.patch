diff --git setup.py setup.py
index 32ed39d..678637e 100644
--- setup.py
+++ setup.py
@@ -38,7 +38,6 @@ except IOError:
     README = CHANGES = ''
 
 install_requires=[
-    'setuptools',
     'WebOb >= 1.3.1', # request.domain and CookieProfile
     'repoze.lru >= 0.4', # py3 compat
     'zope.interface >= 3.8.0',  # has zope.interface.registry
