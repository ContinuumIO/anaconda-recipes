package:
  name: s3fs
  version: 0.0.6

source:
  git_url: https://github.com/dask/s3fs.git
  git_tag: 0.0.6

requirements:
  build:
    - python
    - setuptools
    - boto3
  run:
    - python
    - boto3

test:
  imports:
    - s3fs

about:
  home: http://s3fs.readthedocs.org/en/latest/
  license: BSD
  license_file: LICENSE.txt
  summary: convenient Filesystem interface over S3
