package:
  name: cmake
  version: "3.3.2"

source:
  url: http://cmake.org/files/v3.3/cmake-3.3.2.tar.gz   [unix]
  fn: cmake-3.3.2.tar.gz                                [unix]

  url: http://cmake.org/files/v3.3/cmake-3.3.2.zip  [win]
  fn: cmake-3.3.2.zip                               [win]

test:
  commands:
    - cmake --version      [not win]

about:
  home: http://www.cmake.org/
  license:  3-clause BSD
  license_family: BSD
  summary: CMake is an extensible, open-source system that manages the build process
