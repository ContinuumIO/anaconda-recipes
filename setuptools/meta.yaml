package:
  name: setuptools
  version: 20.7.0

source:
  fn: setuptools-20.7.0.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools/setuptools-20.7.0.tar.gz
  md5: 5d12b39bf3e75e80fdce54e44b255615
  patches:
    - nodownload.patch
    - setup.patch

build:
  entry_points:
    - easy_install = setuptools.command.easy_install:main

requirements:
  build:
    - python
  run:
    - python

test:
  commands:
    - easy_install --help
  imports:
    - setuptools
    - easy_install
    - pkg_resources

about:
  home: https://pypi.python.org/pypi/setuptools
  license: PSF or ZPL
  license_family: PSF
  summary: Easily download, build, install, upgrade, and uninstall Python packages
